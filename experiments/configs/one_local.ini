leader_id = 0;
/* restore_interval = 3; */
variants = (
	{
		id = 0;
		address = "127.0.0.1";
		port = 7773;
		/* // The following shows an example of how to define
		   // breakpoints at which checkpoints will be created: 
		breakpoints = (
			{
				interval = 100;
				// The following is the offset of the write()
				// call in experiments/simple_program/main
				// ..on eiger:
				pc = 0x400794; 
				instr_len = 4;
				// ..on blackforest:
				/* pc = 0;
				instr_len = 5; */
			}
		) */
	}
);
